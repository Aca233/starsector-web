# 已发射弹丸的短时规避规划（2026-09-18）

**NOT_DEPLOYED_CURRENT_ENGINE_PERFORMANCE_FAIL**。本规避算法未接入正式源码，RL仍关闭。另行完成的导航/推进指令正确性修复已接入，不能与本候选的战力结果混算。

## 依据而非猜测

沿用已诊断的五舰seed1601，在30–40秒记录真实碰撞扣血。Sunder损失来自非制导的已发射弹丸：自动脉冲炮、轻型针刺、重型迫击炮、轨道炮；不是只凭WEAPON风险推测。原策略与上轮已拒绝的径向撤退分支均完成带/不带探针完整重演一致。这个结果支持研究弹道规避，但不证明已经有可规避空间或更强策略。

## 本次候选不是撤退阈值补丁

保留原目标、集合/撤退目的地与火控。原导航之外，采样1.5秒内的本舰运动，调用原生操纵/运动方程，分别试减速、停车和±30/60/90度速度。用真实船体多边形与当前弹道的相对扫掠预测潜在命中，至少减少25标称伤害且减少25%，并保留原生避碰风险不恶化，才选择规避；偏离原路线计入代价。规避时不新激活会打断动作的进攻机动系统。

只处理已观察到、非制导、按射程存续且没有近炸引信的线性弹丸；明确跳过导弹、相位舰、固定站/模块、熄火或强制运动系统、玩家显式命令。没有队伍/舰型ID特判，没有修改护盾、伤害、武器安全或手动控制。依赖均来自现有worker协议字段，未修改Protocol。

这仍是近似评分：1.5秒内并未模拟将来发射的炮火、导弹制导、护盾拦截、掩护舰提前截弹或敌人后续决策；评分是标称潜在船体接触伤害，不是经过装甲/护盾后的真实扣血承诺。24个冻结运动夹具中，0.1秒采样与1/60秒控制积分的最大位置误差10.2844。所有shadow运动只写私有位置/速度/转向与输入，不修改真实舰船或随机状态。

## 同状态分支与检查

同一原策略前32秒，随后仅team0启用。Sunder死亡采样由35秒延到36秒，但两局仍负，总战斗60.77→67.40秒，敌方剩余船体比例46.51%→48.12%。因此**该分支不是战力胜利**。238个舰帧应用规避，完整带/不带探针summary和原共同前缀一致。下游随机数消耗可以因轨迹改变而分歧。

11项规避检查、24项原AI基础、12项撤退检查及TS检查通过；包括实际序列化worker/串行控制一致性。夹具初始化的只读isStation改为测试用属性定义，不修改候选规则。

## 冻结实验

基线8e48b524259cd78f6d94fbc78ab9079e3bb727c66190c33ba9afeaf9b8775feb，候选6b32601a1fe736fb8f460545e2e7056c6936f02e1f056387a715979b5b9ccab3。开发6编队×4种子×两边=48场、90秒；总体>50%、每编队>=37.5%才进入另行封存的独立验证。失败不调参追加或删场景，禁止直接部署。

证据目录artifacts/ai/predictive-evasion-2026-09-18/。

## 开发结果与后续封存

48场：10胜6负32超时，计分54.1667%，通过探索门槛。轻型62.5%、驱逐68.75%、突击43.75%、三舰62.5%、五舰43.75%、巡洋43.75%。独立重建审计及首个开发场景两边完整重演通过。**不是已经证明增强。**

独立核心先冻结：9编队×32新种子×两边=576场，180秒；从历史计划排除468个已登记种子，配对和种子整块bootstrap95%下界都须>50%，各编队>=37.5%。扩展保留集同时封存：4个不同编队族×16新种子×两边=128场，核心失败绝不运行；均分>=50%、两区间下界>45%、各编队>=37.5%的防退步检查，不与核心池化。审计完整重演夹具也已预选。

轻型8秒无画面性能探针（与开发CPU负载并行）原540.00ms、候选1145.13ms，约2.12倍；不能忽略性能代价。核心和扩展均过后才运行已封存16舰性能检查：本机无其他本实验worker时，候选完整物理/操纵帧中位<=8.333ms、p95<=16.667ms、均值<=原2.5倍。计时插入实验FleetCombatLab内部，不覆盖会破坏原生优化身份检查的engine方法；必须先证明计时探针完整回放中性。仅无画面CPU指标，不保证浏览器所有渲染场景60FPS。

正式原AI的完整ai:check（原72项）及lint通过，实验检查没有加入生产检查列表。同状态分支34秒Sunder真实船体430，原172；这是局部减伤证据，但仍然败北，不用它替代独立舰队结果。核心会话44711已正常结束；独立审计20978已通过。


## 独立核心结果（已完成并审计）

576场：**305胜、122负、149超时**；超时半分的平均得分 **65.8854%**（不是胜率；实际胜率52.9514%）。配对bootstrap95%区间[62.6736%,69.0104%]，种子整块区间[62.9340%,68.8368%]。九编队最低47.6563%，均满足封存下限37.5%；strengthGate=true。完整记录/配对、精确helper及两处调用点重建、预选4场完整重演和1组全旧中性比较全部PASS。该结果支持冻结候选在核心测试分布上优于原AI，不覆盖所有未知对手。

原封存128场不同编队保留集已开始（会话57371）；不与核心合并，不改变门槛或策略。保留集审计、正式性能、当前源码整合等价性仍未完成，仍NOT_DEPLOYED。并发ThreatAssessment提前排除优化已另外保存证据；源代码预演在版本门禁安全停止，尚未把当前引擎认定为冻结引擎。新发现的推进系统/导航指令冲突在独立目录decision-contracts-2026-09-18处理，不混入此实验。


## 扩展保留集结果（已完成并审计）

128场：**36胜22负70超时**；平均计分55.46875%。配对95%区间[50.390625%,60.546875%]，种子整块区间[51.171875%,59.765625%]；预设非退步门槛通过。资本护航53.125%、航母屏障50%（32场全部超时，不能据此声称打赢航母）、相位混编46.875%、八舰宽阵71.875%。这不是每类舰队都更强。审计完整重演与全旧中性检查PASS，不与核心池化。

封存性能测试已启动（会话13630），启动前本任务的对打/审计/构建/回放进程均已终止，无其他本实验worker。正式源码另行接入了指令所有权bug修复且检查/构建通过，但**没有启用本规避候选**；源码接入审计今后还必须包含该修复与并发随机数/粒子/ThreatAssessment变更，不能覆写回旧引擎。


## 接入门禁最终结果：当前引擎性能不通过，未启用规避

旧冻结引擎上，封存16初始舰测试通过：候选平均7.6865ms、中位7.1273ms、p95 13.4691ms，平均耗时为基线2.4641倍。

但生产引擎已包含其他任务的 ThreatAssessment 提前排除优化、随机数保留采样接口、粒子拆分，以及本任务独立接入的指令所有权修复。为避免直接套用旧数据，保持这些代码，用相同阈值/种子/16舰场景/3秒预热/10秒测量/4轮交错，另做一次当前版本接入性能检查：

| 当前引擎 | 平均完整物理/控制帧 | 中位 | p95 |
|---|---:|---:|---:|
| 不启用规避 | 2.1656ms | 2.0573ms | 3.2160ms |
| 原样接入规避 | 6.5139ms | 6.0642ms | 11.0832ms |

绝对中位和p95通过，但平均耗时比为 **3.0079倍 > 2.5倍**，因此接入门禁失败。新版规避的绝对时间比旧测量更小，但基线也更快，不能因为绝对时间看起来可用就取消已封存的相对开销门槛。这些是本机headless测量，不是浏览器FPS或对每个性能优化的因果归因。

门禁前已验证：当前覆盖层helper只有导入路径归位、策略与参数未改；预先选择轻型、五舰、资本护航、相位混编四种完整180秒场景，对原/当前无规避与原/当前有规避共16次回放，完整摘要逐项相等。性能场景的原/当前摘要也一致。10项当前覆盖层合同与新helper虚拟解析的完整TypeScript检查通过。这些回放是版本审计，不新增战力样本，也不证明所有自定义系统都等价。

当前性能脚本首次在开始模拟前被哈希门禁拦住（自动生成脚本的动态文件名仍指向旧bundle）；保存setup-error日志并修正路径后才执行一次测量。没有看了失败计时再重跑、换阈值或丢弃慢轮次。两次性能报告的样本数、交错顺序、摘要、中位、p95、均值和门禁均另行重算，audit PASS。

**正式源码保持只有独立指令修复，不包含 ProjectileEvasion；生产 ai:check 为84项。** 新规避回归脚本仅保存为 staged-check-ai-evasion.mjs 模板，不将依赖尚未接入helper的脚本留在生产检查里。

下一步只允许行为等价的性能优化：不要缩短预测、减少候选、降低检查频率、删场景或按舰型关闭来救结果。已检查到预测中 driveVelocity 重算getMotionStats，而 advanceShipMotion 另行直接调用shipMotionStats；单纯替换shadow.getMotionStats并不能消除全部重复。若要复用，必须证明一次shadow预测内运动参数固定，保留未知/动态stat回调的原调用语义，并通过逐步路径与完整回放后，才能测真正新优化版本。当前未实现或启用缓存。

最终记录：deployment-decision.json、performance-audit.json。所有对打、审计、计时进程已结束，不留等待中的同一实验需要重启。
