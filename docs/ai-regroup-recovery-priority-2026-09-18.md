# 高幅能撤退与集合目的地优先级实验（2026-09-18）

**REJECTED / NOT_DEPLOYED**。RL关闭，正式CapitalShipAI未修改。

## 单一候选

仅在已有内部撤退、幅能仍高于原resumeAt=65%、有有效目标且已分配REGROUP时，使用原有直接远离目标的撤退速度，而不前往集合锚点。原90%进入/65%解除阈值、正常集合、玩家命令、无后退/突击/撤出系统优先级均不变；没有修改武器、护盾、物理，也不组合先前被拒绝的策略。

基线8e48b524259cd78f6d94fbc78ab9079e3bb727c66190c33ba9afeaf9b8775feb；候选75e88bd61f7c26aaeaf41b0b17d15ca31d48216a26ff8f96a1f9b235fcb3177a。保留同期AutofireController优化。

## 同状态分支

五舰seed1601，前32秒保持原策略，从32秒仅team0启用候选。33/34秒的正向集合指令确实改为-90撤退，但Sunder船体反而590/132，原为728/172，二者都在35秒采样前死亡。两局均负：原60.77秒，分支61.58秒；存活稍长不算变强。所有带探针/无探针完整summary及共同前缀一致。后续下游随机数消耗不保证完全相同。

## 检查与开发门槛

10项新合同（含命令/手动/多队路由/worker一致性）、24项原AI、12项撤退检查通过。最初死亡夹具仅写hullHp=0但未设置isDead，修正夹具为引擎实际死亡标记；不改候选来满足错误夹具。

开发计划先冻结：6编队×4既有开发种子×两边=48场，90秒。须总体计分>50%、每编队>=37.5%。失败即拒绝，不调参/追加/删场景；通过也仅允许另外封存独立验证，不直接部署。

证据目录artifacts/ai/regroup-recovery-priority-2026-09-18/。

## 最终结果

48场/24配对：7胜、11负、30超时，计分45.8333%。轻型43.75%、驱逐50%、突击43.75%、三舰50%、五舰37.5%、巡洋50%。总体未过开发门槛，**拒绝且不部署，不运行独立验证**。

审计通过：冻结哈希、单表达式候选和分发器重建精确一致；48条记录/24配对统计正确，首个注册场景/种子两边完整重演一致（不计额外样本）。正式CapitalShipAI与冻结前一致，RL=false；lint通过。开发会话84479与审计会话54752均exit0。

这说明“高幅能撤退不该被集合覆盖”是待检验的战术判断，不是已证实的增强。分支中沿目标径向直接退后反而更易受伤，不能据一个内部状态冲突就大改正式行为。后续应研究实际来火方向、横向逃逸和可用掩护，而非继续调这条被拒绝的径向规则。
