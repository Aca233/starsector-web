# Weapon Fidelity Parameter Mapping

This file records source-to-web mappings that are confirmed enough to implement. It deliberately separates data transport from visual interpretation.

| Source concept | Web contract / renderer | Status | Notes |
| --- | --- | --- | --- |
| projectile length / width | `WeaponSpec.projLength/projWidth` -> `Projectile` -> projectile render pass | implemented | TPC and Autopulse now use per-ID visual profiles with `trailScale=1` and `glowScale=1`, so confirmed source dimensions are not enlarged a second time by the TPC family profile |
| RGBA projectile fringe/core | `fringeColor/coreColor` | implemented for source-authored projectile strips | WebGL preserves configured alpha instead of discarding the fourth channel. A weapon-animation `.wpn glowColor` (for example TPC mount glow) is not reinterpreted as projectile glow; a projectile `.proj glowColor/glowRadius` (for example Heavy Blaster) is a separate source-authored in-flight halo contract |
| signed texture scroll | `textureScrollSpeed` | implemented for pulse, beam and source ballistic strips | The production WebGL combat renderer uses the configured sign directly for source-authored strip UV phase. |
| texel density | `pixelsPerTexel` | implemented contract and WebGL strip sampling | Validated as strictly positive before registration. The WebGL strip path maps source texels to world width without stretching; e.g. a 128px texture at `5` spans 640 world units, while TPC/Autopulse at `1` consume only the source texels required by their 100/50-unit lengths. |
| pulse fringe/core geometry | one source `length/width` and one `textureScrollSpeed` | implemented for TPC/Autopulse pulse path | Inspected rough/smooth fringe and core textures are all 128×32; the core textures already contain a narrower alpha footprint, so the renderer no longer shrinks core geometry or accelerates core UV independently |
| projectile hit glow | `.proj hitGlowRadius` -> `WeaponSpec/Projectile.hitGlowRadius` -> `HitGlowAnimation` -> WebGL `hit_glow.png` | implemented as an independent visual channel | Source radius is no longer converted into an explosion/fireball radius. Autopulse `50`, Mark IX `50`, Heavy Mauler `80`, HVD `100`, Heavy Blaster `75`, Light MG `15`, Flak/Dual Flak `60`; TPC remains undefined because its `.proj` does not define the field. The Web fade envelope remains PROVISIONAL because static data does not define its exact runtime persistence |
| projectile fade time | `fadeTime` | data contract implemented only | Copied to real and synthetic projectiles and validated as non-negative. Visual residual lifetime semantics are intentionally not implemented until reference behavior is confirmed; collision lifetime is unchanged |
| per-weapon overrides | `WEAPON_VISUAL_PROFILES_BY_ID` | source-aware overrides implemented | TPC and Autopulse neutralize family-only muzzle/glow/trail/impact/core/brightness multipliers so their authored `.wpn/.proj` geometry and alpha are not amplified a second time. Generic family profiles remain fallback behavior for weapons without a closed static-source contract |
| TPC mount glow placement | source `turretGlowSprite` / `hardpointGlowSprite` -> Registry -> WebGL ship pass | implemented | TPC source has no turret glow sprite and has only `onslaught_thermal_glow.png` for the hardpoint. Registry no longer aliases that hardpoint glow into the turret field; the WebGL ship pass consumes `hardpointGlowSpriteUrl`. |
| ballistic source body dimensions / RGBA | `projLength/projWidth`, `fringeColor/coreColor` -> real `Projectile` -> WebGL projectile pass | implemented for source-authored BALLISTIC projectiles | Mark IX is `40×7.5`, Heavy Mauler `50×9`, HVD `120×7.5`; Flak/Dual Flak use their source dimensions. With `pixelsPerTexel` present, WebGL submits one source length/width for `projtrail/projbody`, relies on texture alpha shape, preserves RGBA, and suppresses the legacy synthetic tip glint. |
| ballistic fade / scroll / texel metadata | `fadeTime`, `textureScrollSpeed`, `pixelsPerTexel` | scroll/texel rendering implemented; fade remains metadata-only | Source ballistic strips consume signed scroll and texel density in the WebGL combat renderer. `fadeTime` still does not extend collision/lifetime and its residual visual semantics remain pending runtime reference evidence. |
| source projectile style transport | `spawnType` + `visualSpawnType` -> real/synthetic `Projectile` -> simulation/renderers | implemented for Light MG and Heavy Blaster | Light MG and Heavy Blaster now use the source `BALLISTIC_AS_BEAM` simulation contract; `visualSpawnType` remains an independent renderer selector so visual-family treatment never mutates collision semantics. Swept segment collision provides the RAY-style gameplay path. |
| PD / flak source fields | per-weapon dimensions, RGBA, fade, scroll, texel density, muzzle particles + mechanics-owned burst/fuse fields | implemented for Light MG, Flak, Dual Flak | Light MG `35×3.5` and now fires the source five-shot `0.1s` burst; Flak `35×6.5` uses `35/50/35` fuse radii; Dual Flak `35×4.5` uses `15/30/15` with core/full-damage then linear falloff. Dual Flak also restores the `40 / 25 / 8 / 8 / 0.12 / 46` muzzle-particle spec. |
| beam source width / material | `beamWidth`, `fringeColor/coreColor/glowColor`, `textureScrollSpeed`, `pixelsPerTexel` -> `WeaponSpec` -> real `Beam` -> WebGL beam renderer | implemented for Tachyon Lance, Graviton Beam, Tactical Laser | confirmed source widths are `25 / 20 / 13`; scroll speeds `292 / 260 / 72`; all three `.wpn` files use `pixelsPerTexel=5`. Per-ID visual profiles keep confirmed geometry at 1× instead of applying the generic Beam family geometry multipliers again |
| beam fringe/core geometry | one source `.wpn` width and one scroll speed | implemented for the three W05 source beams | inspected rough fringe/core textures are both 128×32 while the core texture already has a narrower alpha footprint. When source texel density is present, fringe/core now use the same submitted width and scroll speed; the texture alpha footprint performs the visible core narrowing instead of a second `coreScale` or `1.35×` UV speed |
| sustained beam continuity | `beamVisualMode=SUSTAINED` + firing-cycle id + persistent same-slot Beam + `BeamVisuals.beamVisualTime` | implemented in simulation and renderer | Graviton/Tactical Laser now own one Beam entity per trigger-held firing cycle; ACTIVE refreshes the same entity and release refreshes it into non-damaging chargedown. UV phase remains combat-time based. Renderer duplicate collapse is only a defensive fallback. |
| beam firing lifecycle | `beamSourceChargeupTime/beamDuration/beamSourceChargedownTime/beamBurstDelay`, `fluxPerSecond`, optional ammo/regen | implemented in fire control | Tachyon runs `0.5/1.0/1.0/4.0`; Graviton/Tactical use `0.1s` in/out around a trigger-held ACTIVE state; Burst PD uses `0.1/0.3/0.1`, `0.1` delay, four ammo and `0.5/s` regen. Chargedown keeps the beam visual but sets `damageActive=false`. |
| launcher smoke | `launcherSmokeSpec` -> real fire callback -> `CombatFXSystem.spawnLauncherSmoke` -> muzzle-particle render paths | implemented for W06 missile batch | Launcher smoke is a separate visual contract from additive muzzle flash. Source smoke particles use normal blending and visual RNG; they do not alter fire timing, recoil, projectile velocity, or combat RNG |
| missile engine geometry | `missileEngineVisualSpec` -> `WeaponSpec` -> real `Projectile` -> WebGL missile renderer | implemented for Reaper, Annihilator, Sabot, Atropos | nozzle offset, width, length, RGBA, glow multiplier and optional alternate glow color are renderer-only. They replace Typhoon-ID-specific flame/glow sizing without changing engine acceleration or max speed |
| persistent missile trail | `missileTrailSpec` -> real `Projectile` -> `MissileGuidanceHandler` -> `ContrailEngine` -> WebGL ribbon renderer | implemented for W06 missile batch | duration, width, widening, minimum segment distance, spawn offset, RGBA and `NORMAL/GLOW` blend are validated before registration. Trail state remains visual-only. |
| Reaper compact body / red GLOW trail | `projLength=23`, `projWidth=14`, `fadeTime=0.5`, compact sprite + engine/trail contracts | implemented on `WPN-MSL-01` | the scene uses the actual Typhoon/Reaper launch path and verifies `isGuided=false`; mechanics use source launch `100`, acceleration `500`, max `400`, `3.5s` flight lifetime and HP `500`. Source `fadeTime` remains presentation metadata separate from collider lifetime. |
| guided missile evidence | Atropos source body/engine/trail + source-aligned guidance | implemented on `WPN-MSL-02` | the Dagger's native Atropos launcher fires against an offset target with source launch/max speed, lifetime, HP, acceleration and acceleration-limited turn rate. This demonstrates the authoritative guided path without using an unguided Reaper as a turning sample. |
| rapid rocket / staged missile evidence | Annihilator `WPN-MSL-03`, Sabot `WPN-MSL-04` | implemented through real fire control | Annihilator covers repeated launches with source cadence/kinematics/lifetime. Sabot uses the source parent guidance/lifetime and replaces the old velocity-boost stage with five deterministic `sabot_warhead2` children carrying `200 kinetic + 200 EMP` each. |
| Heavy Blaster source projectile style | `spawnType/visualSpawnType=BALLISTIC_AS_BEAM`, `45×7`, RGBA, fade `0.3`, scroll `64`, texel density `5`, `.proj glowColor/glowRadius=35`, source sprite/muzzle resources | implemented on `WPN-HBLASTER-01` | source rendering geometry is kept at 1×; WebGL consumes the independent in-flight glow and direct tests prove radius `35 -> 0` removes its draw call. Gameplay uses the source RAY-style spawn contract with swept segment collision and source turn rate `15`. |
| Burst PD source beam material | width `17`, ROUGH, scroll `128`, `pixelsPerTexel=5`, source RGBA, `hitGlowBrightenDuration=0.25` and mount resources | implemented on `WPN-PDBURST-01` | real Doom `WS 009` fire reaches BeamSimulation and shield contact. The source brighten duration modulates only contact-glow alpha. The target beam `.wpn` files do not define `hitGlowRadius`, so previous synthetic per-weapon beam hit-radius values were removed and the existing width-based Web fallback is explicitly PROVISIONAL |
| projectile impact surface routing | `ImpactVisuals.getProjectileImpactVisualProfile` + independent hit-glow/missile-explosion channels | implemented structurally | shield, armor and hull remain explicit presentation surfaces. A shield-blocked projectile cannot create a hull/missile fireball; a body-hit projectile may emit source `hitGlowRadius` as `hit_glow.png`, while missile fireball size comes only from the missile's top-level visual explosion fields. Exact spark/ripple intensity remains PROVISIONAL |
| missile top-level visual explosion | `.proj explosionRadius/explosionColor` -> `missileExplosionVisualSpec` -> source-authored `ExplosionAnimation` | implemented for Reaper / Atropos / Annihilator / Sabot | source values are Reaper `350` `[255,100,100,255]`, Atropos `250` `[255,155,100,255]`, Annihilator `75` `[255,165,0,255]`, Sabot `125` `[255,165,0,255]`. The source comment also requires a white additive core over the configured color; WebGL renders that layer. Source-authored explosions suppress generic Web shockwave/smoke/debris additions; exact white-core size/falloff and detailed explosion-particle execution remain PROVISIONAL |
| beam contact brighten provenance | `.wpn hitGlowBrightenDuration` -> `WeaponSpec` -> `Beam` -> contact-glow alpha ramp | implemented for Burst PD (`0.25s`) | This is renderer-only metadata and never changes contact detection, damage ticks, Beam lifetime or audio cadence. Exact perceived interpolation remains PROVISIONAL |
| sustained Beam contact cadence | Beam visual-only `contactSurface/contactFxCooldown/contactSoundCooldown` + fixed-step cadence | implemented structurally in W07 | contact sparks/ripples and `beam_hit`/shield-contact audio are emitted on simulation-time cadence (`0.08s` FX, `0.18s` sound Web defaults) instead of visual-RNG per-tick draws. Damage/contact simulation runs every normal tick and is not throttled |
| EMP visual/audio ownership | `CombatFXSystem.spawnEmpArc` is visual-only; semantically meaningful callers own their sound event | implemented in W07 | removes AudioContext wall-clock throttling as a side effect of drawing an arc. Tachyon mechanical EMP, projectile EMP and Sabot stage already have explicit event sounds; audio pitch randomness remains outside combat and visual RNG streams |

## Explicit non-mappings

- `fadeTime` is not used to keep a projectile collidable after its normal simulation lifetime.
- Visual dimensions are not assigned to damage radius or collision radius.
- Weapon animation `glowColor` is not interpreted as a per-frame projectile halo for `BALLISTIC_AS_BEAM` shots.
- No damage, range, refire, projectile speed, collision, guidance, or beam-damage-duration values were changed in this batch.
- Ballistic source `fadeTime` metadata does not extend the projectile collider or damage lifetime.
- `visualSpawnType` is renderer-only and must not be consumed by collision, damage, range, fuse, or lifecycle systems.
- Beam source chargeup/chargedown metadata does not alter fire-control cadence, damage duration, refire delay, or Beam simulation lifetime in W05.
- Render-time sustained-Beam deduplication is not simulation deduplication and must never be used to suppress damage ticks or sound events.
- Launcher smoke, missile engine dimensions/colors and missile trail parameters are visual-only fields and must not be consumed by guidance, acceleration, collision, damage, fuse, HP, or lifetime logic.
- Missile `projLength/projWidth` and trail width are display geometry; they are not aliases for collision radius, explosion radius, or damage radius.
- Missile top-level `.proj explosionRadius/explosionColor` are stored in `missileExplosionVisualSpec` and are visual-only; they are not aliases for `explosionSpec.radius/coreRadius`, proximity-fuse radius, collision radius or damage radius.
- `.proj hitGlowRadius` is never used as an explosion/fireball radius. It owns only the independent hit-glow layer.
- Target Beam `.wpn` files without `hitGlowRadius` do not receive invented per-weapon source radii; width-based endpoint-glow sizing remains a Web PROVISIONAL fallback.
- Reaper remains unguided in W06. Atropos is the guided-turn acceptance sample; Sabot's current Web two-stage behavior is observed, not silently replaced with source MIRV mechanics.
- Shield impact presentation never substitutes visual fireball size for damage/collision radius, and suppressing a shield fireball does not change absorbed damage or flux.
- Beam contact FX/audio cadence is not a damage cadence: it must never skip or combine Beam damage ticks.
- Heavy Blaster `visualSpawnType=BALLISTIC_AS_BEAM` does not convert its Web projectile collision to source `RAY` behavior.

