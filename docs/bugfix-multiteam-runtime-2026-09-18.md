# 多队仍少显示：运行版本与真实 Worker 复核（2026-09-18）

## 本轮确认

- 默认 3001 服务 `/lan/info` 仍报告协议 17、构建 `2026-09-17T14:34:28.94Z`，早于上次公开战场修复；磁盘 dist 为 `2026-09-17T15:17:58.913Z`，当前源协议已是 19。后台进程捕获的构建标识不会随磁盘输出自动更换。
- 因而上次构建通过不等于用户运行的对局已更新。尚未取得用户当前完整游戏地址，不能断言默认 3001 就是本次报告所在的页面，也不能声称已修好用户正在运行的对局。
- 本轮不继续无根据地修改传感器或吞掉缺失状态。新增队伍编成、已同步、在场、可见、后备、损失、撤离统计，并在帮助菜单显示实际页面构建 / 地址 / 视野模式。后备舰明确显示为待命，不再仅给一个队伍色块；公开战场隐藏活动舰或缺少身份时显示警告。

## 实际同步验证

新增 `scripts/check-multiteam-worker.mjs`，使用独立浏览器，不加入玩家房间：

- 创建一个真人旗舰加四艘异队 AI 的真实五队名单，不把船移近、不直接强制可见。
- 启动真实 `host.worker`，消费其 JSON 消息，经 SnapshotPlayback / applyCombatSnapshot 还原玩家世界。
- 验证连续 61 个数据包、到物理步 180：权威在场 5、客户端在场 5、可见 5，五队身份均保留；页面异常为 0。
- 分别运行开发 Worker 和本轮生成的生产 Worker，均得到上述结果。生产 Worker 的地址可通过 `COMBAT_WORKER_URL` 指定。
- 统计覆盖预算后备、损失、撤离、缺少身份和实际同步帧；TypeScript / lint 通过。

## 新构建和生效状态

生产构建输出到独立的 `artifacts/multiteam-verified-20260918`，标识 `2026-09-17T16:50:51.632Z`（本地 2026-09-18 00:50:51）。构建通过，只有现有的大分包提示。

尝试启动独立 3008 验证后台的命令被执行策略拒绝，未执行；没有宣称该地址已可访问。未停止或重启既有 3001 / 3006 / 3007 服务，没有打断玩家对局。需要先确认用户正在使用的地址，并在同意结束对应旧对局后切换运行后台；不能再仅让用户刷新旧页面就声称修复已生效。
