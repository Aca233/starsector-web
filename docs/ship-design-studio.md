# 舰船改装界面（以原游戏为准）

## 基准与本次纠偏

用户明确要求不要自行设计。视觉基准为提供的原作改装截图；交互依据本地原作界面代码，不再以自创工作台代替：

- `decompiled/starfarer_obf/com/fs/starfarer/coreui/refit/Q.java`：底部武器组、装配方案、撤消、清空装配、修复、模拟战斗、下一艘按钮。
- `WeaponPickerDialog.java`：挂点旁的右侧单列武器列表、左侧详细数据、当前武器、类型筛选、悬停参数、单击换装；不够装配点的武器禁用。
- `wgd2/WeaponGroupDialogV2.java`：842px 窗口、7 组分配、自动开火行、右侧射击模式/幅能表、自动分配、确认/取消。
- `ManageVariantsDialog.java`：当前装配名称、保存当前配置、读取/重命名/删除。
- `ModPickerDialogV3.java`：覆盖左侧舰船列表的展开式插件表格，右侧参数不进入模态遮罩；四列排序、分类筛选、点击安装/移除。

已撤掉自创的左侧工具栏、挂点清单、推荐装配入口。左侧只保留实际可改装舰船，中央/右侧/底部位置继续按截图。首页只有「舰船设计」。现有战斗使用 AuthenticTacticalConsole。

## 操作

- 左侧选舰：未修改可直接切换；只有相对打开时基准或已存方案发生实际修改才询问保存/放弃。仅打开插件、查看武器、打开编组后取消，不产生修改；撤消回初始状态后也不再询问。
- 鼠标移到舰船上显示挂点；左键打开挂点右侧的武器列表，单击安装，右键卸下。顶部当前武器条目可点击拆卸；内置武器不可拆。悬停更新左侧说明，按住 Ctrl 对比当前武器和候选武器，F2 打开原作背景资料。1/2/3 切换武器类型；左键点击外侧、右键或 Esc 关闭。
- 武器说明/战术应用来自原作 descriptions.csv 与 weapon_data.csv，伤害类型图标使用原作资源。当前没有仓库/市场，所以库存来源按钮禁用并有说明，不编造“100可用”等数量。
- 右侧调整幅能和插件；A 在左侧展开插件列表，右侧保持可操作。再次 A、返回按钮或 Esc 只收起列表。展开期间舰船挂点、舰船列表和底部改装操作不接收输入。
- 插件表格按原图保留「舰船插件 / 设计类型 / 装配 / 状态 & 需求」；列标题切换排序。类型来自原作 CSV 的 uiTags，筛选计数仅统计当前已实现的六项，零项分类禁用，不填充虚构条目。
- W 打开七组武器管理：自动开火行在编号表头上方，按武器类型染色，方形勾选格、挂载点/炮塔标签与实际射界图标。Q 自动分配、W/Enter 确认、T/Esc 取消；确认才写回，取消保持原配置。
- V 打开装配方案。保存当前配置；已有配置换名保存会另存为新方案，旧方案不被覆盖。支持读取、重命名、删除。
- T 清空装配，从空舰重新配置；U 撤消。Ctrl+S 快捷保存仍可用。
- G 的按钮恢复原作“修复”，当前无 D-插件损坏，故禁用，不拿保存功能冒充修复。
- N 模拟战斗；试战使用当前真实装备、插件和编组，返回后不保留战损。

## 内容与边界

目前仅包含攻势、典范、厄运；16 种可安装武器与攻势内置 TPC；6 种可编辑插件。这不是完整原版移植：未接入市场/经济、D-插件修复、S-插件、全部原作武器或原作全部自动装配方案。模拟对手选择仍是网页的有限三舰目录，不宣称已还原原作整套部署界面。

不为了对齐截图而伪造数值或把未实现武器替换贴图冒充原武器。默认配置是当前支持装备的可用配置，不标注为原作 stock variant。原作典范“突击”方案使用尚未接入的等离子炮、重型针刺炮、PD 激光，未冒充完整实现。

四个新插件的数据与效果来自本地 CSV / `data/hullmods/*.java`，普通安装（不含 S 插）：稳定护盾维持 ×0.5；强化护盾承伤 ×0.8、穿盾概率 ×0.5；幅散管道扩容排幅 ×1.25、EMP 损伤 ×0.5；幅能配送器主力舰耗散 +150。界面和战斗共用 effectiveHullStats。护盾维持以未加耗散通道的基础耗散计算，避免重复加成。

方案库使用 `starsector-web:design-studio:v1:` + BASE_URL，最多 100 份；草稿单独缓存。旧五组草稿保留原分组并追加两组空组。保留未知版本、损坏数据、跨标签页写入保护与 WebGL 失败返回。试战是隔离 GameSession，不写原作和舰队存档。

## 本次核验

独立临时浏览器，不修改用户浏览器资料；未重建测试文件/测试框架，未做手动资源哈希审计。

- 自创侧栏组件不再渲染；同尺寸主界面与原作截图人工对照。
- 修复透明插件区域吞掉右侧挂点点击的问题，检查 23 个挂点中心的命中目标。
- 单击武器安装与实时 OP；七组取消不改变原配置，确认后第七组真实传入战斗。
- 保存两份不同名称配置并保留旧配置，重命名和读取；刷新后配置存在。
- WebGL2 状态 ready，插件实际值符合模型，试战返回释放会话且保留装配。
- 原有造船/清空、撤消、超预算拦截仍有效。


## 两张补充原图的对齐与核验

以用户提供的武器组管理、舰船插件展开截图为本轮布局基准；保留当前真实武器与舰船数据，不将截图上的装备和数值写成常量。

- 武器组：原作八片边框、居中标题带、21px 彩色行、7 列青色方格、右侧射击模式/幅能、底部 Q/W/T 按钮。窗口随实际已装武器行数调整，少于 15 行保留最小列表高度。
- 插件：1048×788 下表格起点约 (27,42)，宽约 665px；设计类型筛选起点约 (34,583)。右侧参数位置保留，非居中弹窗。悬停说明不占固定详情侧栏。
- 隔离浏览器检查安装/卸下、左右状态同步、护盾筛选两项、装配点排序、不兼容拦截、右侧幅能加减；A/Esc 收起且不退出改装，隐藏的 T/U/X/N/W/R 操作不触发。
- 稳定护盾卸装验证 OP 370→355→370、护盾维持 375→750→375。
- 编组 T 取消不写回，Q 自动分配不绕过确认；W 确认的第七组真实进入战斗（WS 001、交替、关闭自动开火）。WebGL2 ready，插件耗散 1900、护盾维持 375、排幅倍率 1.25；结束试战释放会话，刷新后配置保留。
- 本轮检查中无浏览器 pageerror；通过 typecheck、lint 和 build。未新增测试脚本、测试框架或手动资源哈希审计。

## 换武器原图与交互问题修正

- 修正插件展开时额外设置左边距导致窗口缩窄：展开/收起的 refit-shell 与 refit-grid 边界完全一致，不再移动主窗口。
- 修正 `!saved` 把未保存的新舰船误判为已修改。新增随草稿缓存的 baseline；打开方案/切换舰体/保存时更新基准。旧缓存回退到对应默认舰体作比较，已有保存副本仍优先比较副本，避免漏掉真实未保存修改。
- 新的武器选择采用 380px 右侧面板、左侧最多 420px 原始/辅助数据。悬停说明、原作伤害图标、当前安装高亮、六个来源/类型按钮位置参照补充原图，不使用居中卡片弹窗。
- 修复 360° 射界用单条 SVG 弧造成起止点重合而不显示的问题；选择武器时显示灰白实线射界。
- 隔离浏览器核验：三舰连续无修改切换无确认；真实改动切船会确认，取消保留、撤消回原装不提示；刷新后真实编辑仍保留保护，保存后直接切换。
- 核验武器类型筛选、当前武器卸载、单击换装、装配点不足拦截、Ctrl 对比、F2/返回、左键外侧关闭和右键关闭；1048×788 与 1438×844 下左右面板不重叠且图片加载成功。未产生 pageerror。


## Esc 暂停菜单（原作截图）

- 移除战斗右上角工具栏，战斗中按 Esc 打开 500×292 原作布局：原作八片边框、左侧当前舰船及实际装备预览、右上「游戏设置」、右下「结束模拟 / 返回游戏」。不把截图中的锤头级写死为当前舰船。
- 菜单打开时暂停模拟并清除按住的移动/开火输入，关闭后恢复进入前的状态；空格暂停不会因打开/关闭菜单而解除。
- 游戏设置提供已实现的默认鼠标转向、静音和操作说明；设置存于浏览器，静音实际连接主音频总线。开发者旧战斗入口的舰船资料、舰队/存档从此进入，不增加试战的战役功能。
- Esc 只关闭最上层窗口；长按不反复切换。修复点击暗色遮罩后焦点落到 body 导致 Esc 失效的问题，最高层弹窗在捕获阶段接收 Esc。
- 「结束模拟」直接释放隔离战斗会话，返回改装；草稿、装配和保存基准不受战损影响。
- 独立临时浏览器已核验：无战斗工具栏，首次准备完成后 Esc 打开；菜单/设置/操作说明期间战斗时间不变；Esc 逐层返回、点击返回、空格暂停保留、长按防抖、点击遮罩再 Esc、清除开火、鼠标转向/静音开关、结束后会话 disposed 且草稿不变；无 pageerror。
- 外观记录：`artifacts/native-refit/escape-pause-reference.png`。未新增测试框架或测试文件。
- 追加核验：修复资源准备弹窗卸载后旧 Esc 监听短暂残留的问题；三次全新进入模拟均第一次 Esc 成功，长按保持菜单。1048×788 下菜单保持 500×292 居中；旧战斗入口的舰队/存档可从设置打开并按 Esc 返回。typecheck、lint、build 通过。


## 原版皮肤改为全站共用

暂停菜单与设计页不再各写一套皮肤。NativeChrome 统一原版八片边框、按钮和扫描线/噪点材质；NativeBitmapText 使用原作中文动态字图，根字体不再优先使用雅黑。武器/插件筛选、武器编组、装配方案和通用弹窗均已接入；窗口大小、数据和原有操作保留。实现规范与实测结果见 [共用原版 UI 基础层](native-ui-system.md)。
