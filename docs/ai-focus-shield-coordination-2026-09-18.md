# 集火与护盾的组合规则实验（2026-09-18）

## 状态

**REJECTED / NOT_DEPLOYED**。正式AI未修改，RL仍关闭。目标仍是实测更强的AI，而不是只完成验证脚本。

## 与前两个已拒绝候选的关系

A为原火力需求估计填满之后才开始投入扣分，B为非相位盾成本预算只计算已发射弹丸/当前光束。两条规则原参数和表达式保持不变，不选择有利编队才启用。先前A、B各自独立门槛均失败，仍是拒绝状态。

本次是新的**组合策略**及其交互研究。旧结果只作开发信息，不能重新称为未见验证，不能把两个点估计相加当组合胜率，不能借组合测试给单项改判。

## 当前引擎隔离

工作区同期有ShipCombatProfile的保守角度快速排除优化；没有回退它，也不把它算作AI战力收益。当前基线`22840b5485646dc1fffe5d7ffbcd74a2a276849ba8418e495d82defd00971e03`，组合候选`a6ea1e7f44cc8a26545df889d6573f0c6a5233f0139e1cec5542022f6085050c`。候选只改变FleetTactics和ShipDefenseController中各一个表达式；当前相同物理/内容用于所有条件。

最初初始化因旧基线哈希不匹配而停止，未生成候选或启动有效对打。development.log保存该失败启动，实际开发日志为development-run.log。

## 三条件开发对打

每条件6编队×4既有开发种子×两边、90秒，48场，总计144场新模拟，未与旧记录混合。

| 条件 | 胜 / 负 / 超时 | 计分 |
|---|---:|---:|
| 仅集火 | 18 / 6 / 24 | 62.50% |
| 仅护盾 | 10 / 8 / 30 | 52.08% |
| 组合 | 9 / 4 / 35 | 55.21% |

组合是**9胜4负35超时，计分55.21%，实际胜率18.75%**。它弱于开发中的仅集火；每配对的描述性交互AB−A−B+0.5平均为−9.375个百分点，**不支持“正向协同”说法**。但相对正式原AI仍有探索性正向，按事先开发入选条件继续独立验证，而不是已经采用。驱逐编队组合37.5%、三舰43.75%均保留。

96场当前引擎上的A/B控制条件与历史旧引擎完整summary逐场精确一致（仅标准化场景标签）。这支持该批回放的同期优化等价，不是所有输入的形式证明，不增加独立样本数，不算本任务增强。

## 正确性与分发

8项集火合同、9项护盾合同、24项原AI基础、12项撤退测试通过。另有3队敌我/队伍编号置换、玩家命令/手动归属与隐藏目标检查。72个同状态舰队路由夹具、144个护盾路由夹具、4个单组件完整对打重演、2个全旧完整重演通过。它们只证明所测实现正确，不证明战力。

## 核心独立验证（先冻结后运行）

新组合对正式原AI，9编队×32新种子×两边，180秒，**576场/288配对**。32个种子块是本新组合第一次验证开始前固定的设计，不是追加以前A/B的验证样本。开发及所有历史结果不混入。

须总体计分>50%，配对与种子整块bootstrap95下界均>50%，每编队>=37.5%，并通过哈希/路由/物理/控制/重演审计。否则拒绝，不调参、不加样本、不删弱场景或延长时限。

## 扩展场景保留集（已封存，核心通过才运行）

另有4个未运行的新编队族：主力舰护航、双航母屏护、相位混编、8舰宽阵；16个不同新种子，两边、180秒，共128场/64配对。只作为预注册泛化防退步检查：均分>=50%，两种95%下界均>45%（5个百分点非劣界），各编队>=37.5%。**这是防退步门槛，不是宣称每个新场景都增强或普遍不退步。** 核心增强门槛仍不可替代；不池化两批来救任意一个门槛。

正式整合前还需核对最新源代码、仅应用两个冻结表达式、完整AI检查/lint/build、部署bundle与冻结候选行为一致。FFA只检查控制正确性，未声称多队混战胜率提升。

证据目录artifacts/ai/focus-shield-coordination-2026-09-18/。主要文件：isolation.json、development-plan.json、development.json、routing-audit.json、concurrent-optimization-replay-parity.json、validation-plan.json、pre-result-audit-plan.json、holdout-plan.json、holdout-audit-plan.json。

## 最终结果与决定

576场/288配对：235胜、181负、160超时，计分54.6875%。配对95%区间[50.9549%,58.4201%]，种子整块95%区间[51.3889%,57.9861%]。但三舰17胜40负7超时，计分32.03125%；五舰17胜43负4超时，计分29.6875%，均低于预注册37.5%单场景底线。**拒绝，不部署。** 不用总体均值掩盖退步，不按舰型/场景选择启用，不调参续跑或追加样本。

审计通过：576条记录、288配对、216路由探针及3组完整重演；冻结哈希、两个表达式及分发器重建精确一致。正确性通过不改变战力门槛失败。核心未通过，扩展保留集按原计划不运行。正式源代码未由本实验改动，RL仍关闭。validation会话67179和audit会话49604均已exit0，不应重启。
